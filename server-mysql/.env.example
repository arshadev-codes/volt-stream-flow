# Server credentials. NEVER commit a real .env to source control.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=change-me
DB_NAME=reactor_testing

# HTTP server
API_PORT=4000
# Comma-separated list of allowed origins for CORS (use * for dev)
CORS_ORIGIN=*
